<view class="book-catalogue">
<view class="book-catalogue-item" wx:for="{{showPageData}}" wx:key="index" data-id="{{item.uuid}}" catchtap="clickRouterChapters">{{item.title}}</view>
<van-toast id="van-toast" />
</view>
